import { styled } from '@styles/stitches.config';

export const SVG = styled('svg', {
    position: 'fixed',
    top: '75px',
    left: '0px',
    width: '100%',
    height: '5rem',
    color: '$hover',
    zIndex: 999,
})